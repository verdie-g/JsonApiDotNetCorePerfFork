# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Ignore binary which is locally build
./summarize_upload_results

# Ignore changes to Go dependencies, so they are always the latest
# when running go mod tidy
go.mod
go.sum